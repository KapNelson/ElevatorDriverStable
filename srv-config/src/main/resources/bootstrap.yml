spring:
  cloud:
    config:
      name: api-admin
      uri: ${config.server.path:http://127.0.0.1:6002}
      fail-fast: true
      username: ${config.server.username:configUser}
      password: ${config.server.password:configPassword}